import type { NextRequest } from 'next/server'; import { assertSafeMutation, optionsResponse, parseJson, withApiHeaders } from '@/modules/identity/interface/auth-http'; import { requireAuthenticatedUser } from '@/modules/identity/interface/request-authenticator'; import { getGuestService } from '@/modules/guests'; import { guestErrorResponse } from '@/modules/guests/interface/guest-http'; import { invitationQuerySchema, invitationSchema } from '@/modules/guests/interface/guest-schemas'; import { getRequestId, jsonResponse } from '@/shared/http/api-response'; import { weddingIdSchema } from '@/modules/weddings/interface/wedding-schemas'
export const OPTIONS = optionsResponse; type Context = { params: Promise<{ weddingId: string }> }; async function id(c: Context) { return weddingIdSchema.parse((await c.params).weddingId) }
export async function GET(request: NextRequest, context: Context) { const requestId = getRequestId(request); try { const { actor } = await requireAuthenticatedUser(request); const parsed = invitationQuerySchema.parse(Object.fromEntries(request.nextUrl.searchParams)); return withApiHeaders(jsonResponse(await getGuestService().listInvitations(actor, await id(context), { guestId: parsed.guestId, status: parsed.status, limit: parsed.limit, cursor: parsed.cursor })), requestId) } catch (e) { return guestErrorResponse(e, requestId) } }
export async function POST(request: NextRequest, context: Context) { const requestId = getRequestId(request); try { assertSafeMutation(request); const { actor } = await requireAuthenticatedUser(request); return withApiHeaders(jsonResponse(await getGuestService().createInvitation(actor, await id(context), await parseJson(request, invitationSchema)), { status: 201 }), requestId) } catch (e) { return guestErrorResponse(e, requestId) } }
