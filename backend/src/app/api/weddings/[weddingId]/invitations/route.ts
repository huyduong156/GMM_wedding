import type { NextRequest } from 'next/server'; import { assertSafeMutation, optionsResponse, parseJson, withAuthHeaders } from '@/modules/identity/interface/auth-http'; import { requireAuthenticatedUser } from '@/modules/identity/interface/request-authenticator'; import { getGuestService } from '@/modules/guests'; import { guestErrorResponse } from '@/modules/guests/interface/guest-http'; import { invitationSchema } from '@/modules/guests/interface/guest-schemas'; import { getRequestId, jsonResponse } from '@/shared/http/api-response'; import { weddingIdSchema } from '@/modules/weddings/interface/wedding-schemas'
export const OPTIONS = optionsResponse; type Context = { params: Promise<{ weddingId: string }> }; async function id(c: Context) { return weddingIdSchema.parse((await c.params).weddingId) }
export async function POST(request: NextRequest, context: Context) { const requestId = getRequestId(request); try { assertSafeMutation(request); const { actor } = await requireAuthenticatedUser(request); return withAuthHeaders(jsonResponse(await getGuestService().createInvitation(actor, await id(context), await parseJson(request, invitationSchema)), { status: 201 }), requestId) } catch (e) { return guestErrorResponse(e, requestId) } }
